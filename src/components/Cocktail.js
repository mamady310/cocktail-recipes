import React from 'react';
import { Link } from 'react-router-dom';

export const Cocktail = ({}) => {
    return (
        <div>
            Hello from cocktail component
        </div>
    )
}

export default Cocktail;